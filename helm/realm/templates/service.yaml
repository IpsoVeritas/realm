apiVersion: v1
kind: Service
metadata:
  name: realm
  labels:
    app: realm
    release: {{ .Release.Name }}
  annotations:
    {{- toYaml .Values.service.annotations | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.port }}
    name: api
  selector:
    app: realm